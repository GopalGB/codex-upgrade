codex-upgrade 2.3.0
built 2026-06-18
target: OpenAI Codex CLI >= 0.121 (homebrew/npm)
mode: portable, file-based, zero-MCP (hooks optional)
changelog:
  2.3.0 — SWARM: fan one task across multiple expert lenses in parallel via
          isolated `codex exec` workers (--ephemeral, read-only), then synthesize.
          New: swarm skill + /prompts:swarm. install.sh --user-agents-dir (install
          to ~/.agents/skills). CI smoke-on-push (.github/workflows/smoke.yml). MIT LICENSE.
  2.2.0 — OPTIONAL Codex hooks (install.sh --with-hooks): UserPromptSubmit
          frustration-capture + SessionStart law-ritual. /prompts:verify. tests/smoke.sh.
  2.1.0 — GSD default loop (tiered clarity/PRD approval gates), LESSONS.md wtf-log,
          reuse-don't-regenerate, layered-architecture security defaults. /prd, /lesson.
  2.0.0 — initial kit: 8 expert skills, 5 prompts, AGENTS.md law, no-MCP installer.
